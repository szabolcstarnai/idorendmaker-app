# Problems

## Új időrend

- Race list is not scrollable.
  -> It should be scrollable.
- Excel export button is smaller, weird pill under it.
  -> Same size, remove pill with schedule name.
- Clicking on an added race makes a "warning glow"
  -> Clicking on an added race should not do anything.
- The warning glow effect on the first added race is broken. Padding error? The top is not visible.
  -> Fix the glow effect on an added race, so all the sides of the glow is visible.
- Drag and drop on an added race only works when dragging from the timestamp part of the race.
  -> It is not apparent that dragging only works from there. The cursor is "draggy" everywhere. EITHER make the whole "racecard" drag and droppable, or make it absolutely clear that drag and drop only works when dragging the timestamp part.